package com.example.domain.converters


class TaskConverter() {
//    fun fromDbToUi(model: TaskDb): com.example.mydiary.models.Task {
//
//        return com.example.mydiary.models.Task(
//            name = model.name, description = model.description,
//            startHour = start[0].toInt(), startMinute = start[1].toInt(),
//            timerHour = timer[0].toInt(), timerMinute = timer[1].toInt(),
//            date = model.date
//        )
//    }
}